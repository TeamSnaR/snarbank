import { generatedAdminApiTypes } from './generated-admin-api-types';

describe('generatedAdminApiTypes', () => {
  it('should work', () => {
    expect(generatedAdminApiTypes()).toEqual('generated-admin-api-types');
  });
});
